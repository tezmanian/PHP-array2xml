{
    "name": "halberstadt.info/array2xml",
    "description": "Little library to convert an array to XML.",
    "authors": [
        {
            "name": "René Halberstadt",
            "email": "rene@halberstadt.info",
            "role": "Developer"
        }
    ],

    "require": {
        "php": "^7.2"
    },

    "autoload": {
        "psr-4": {
            "Halberstadt\\Array2XML\\": "src/"
        },
        "files": [
        ],
        "classmap": [   
        ]
    },

    "require-dev": {
        "phpbench/phpbench": "^0.13",
        "phpunit/phpunit": "^5.7.27 || ^6.5.8 || ^7.1.2",
        "zendframework/zend-coding-standard": "~1.0.0"
    },

    "autoload-dev": {
        "psr-4": {
            "HalberstadtTest\\Array2XML\\": "test/"
        },
        "files": [
        ],
        "classmap": [
            []
        ]
    },

    "config": {
    },    

    "scripts": {
        "check": [
            "@cs-check",
            "@test"
        ],
        "cs-check": "phpcs",
        "cs-fix": "phpcbf",
        "test": "phpunit --colors=always",
        "test-coverage": "phpunit --coverage-clover clover.xml"
    }
}
